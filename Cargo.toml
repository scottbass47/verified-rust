[package]
name = "verified-data-structures-and-algorithms"
version = "0.1.0"
edition = "2021"

[package.metadata.verus]
verify = true

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
  'cfg(verus_only)',
  'cfg(verus_keep_ghost)',
  'cfg(verus_keep_ghost_body)',
  'cfg(verus_verify_core)',
] }

[dependencies]
vstd = "=0.0.0-2026-09-06-0133"
